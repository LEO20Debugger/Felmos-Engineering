node_modules
.next
# Alternate build dirs from NEXT_DIST_DIR — see next.config.ts.
.next-*
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
next-env.d.ts
.vercel
tsconfig.tsbuildinfo
# Leading-slash paths are anchored to the repo root, so these must be globbed to
# keep matching now that the site lives in web/ (and api/, admin/ alongside it).
**/scripts/.image-cache
**/dist
**/.turbo
# Local stand-in for the Railway media volume.
api/.data
api/.data-scratch

# The project photographs pulled out of the company presentation. ~35 MB of
# originals that exist to be run through `npm run import:deck` once; after that
# the canonical copies are the WebP variants on the media volume. The manifest
# beside it (seed/deck-projects.json) IS committed — it is the editorial record
# of what came from where.
api/seed/deck

# Scratch files holding production credentials, written during deployment
# setup. Delete once the values are in the hosting provider.
VERCEL-ENV.txt
