import { Placeholder } from "../Placeholder";

export const metadata = { title: "Insights" };

export default function Page() {
  return (
    <Placeholder title="Insights" phase="Coming soon">
      The blog. Articles are stored as blocks — paragraphs, headings, quotes and lists — so the editor here will build them piece by piece rather than as one box of formatted text.
    </Placeholder>
  );
}
