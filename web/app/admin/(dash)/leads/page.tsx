import { Placeholder } from "../Placeholder";

export const metadata = { title: "Inspection requests" };

export default function Page() {
  return (
    <Placeholder title="Inspection requests" phase="Coming soon">
      Every enquiry from the contact form will be archived here — searchable, assignable, and with a record of whether the notification email actually reached anyone. Until then, the form still works and still notifies nobody, which is the problem this replaces.
    </Placeholder>
  );
}
