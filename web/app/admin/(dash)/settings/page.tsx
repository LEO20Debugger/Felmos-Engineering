import { Placeholder } from "../Placeholder";

export const metadata = { title: "Settings" };

export default function Page() {
  return (
    <Placeholder title="Settings" phase="Coming soon">
      Phone numbers, address, opening hours, social links — and who receives inspection request emails, including anyone copied in.
    </Placeholder>
  );
}
